#include "main.h"

/**
 * _abs - processes the absolute value of a number
 * @x: has an integer value as an argument
 * Return: absolute value
 */

int _abs(int x)
{
	int k = -1;

	if (x < 0)
	{
		return (x * k);
	}
	else
	{
		return (x);
	}
}
