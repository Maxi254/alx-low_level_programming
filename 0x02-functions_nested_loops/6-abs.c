#include "main.h"
/**
 * int_abs - computes the absolute value of an integer
 * @int - parameter
 *
 * Return: 1 if successful 
 */

int _abs(int)
{
	int n;

	if (int n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
