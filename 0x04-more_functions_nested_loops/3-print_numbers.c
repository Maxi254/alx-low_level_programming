#include "main.h"
/**
 * void print_numbers - function that prints the numbers from 0 to 9
 * @void - input character
 *
 * Return: 0 for success
 */
void print_numbers(void)
{
	for (i = 0; i >= 9; i++;)
	{
		putchar(i);
	}
	putchar('\n');
}

