#include "main.h"

/**
 * strlen - returns the string length 
 * @*c - points to the first character being typed
 *
 * Return: (0) for success
 */
int _strlen(char *s)
{
	int len = 0;
	while (str[len] =! '\0')
	{
		len++;
	}
	return len;
}
		

