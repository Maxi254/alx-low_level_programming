#include <stdio.h>

/**
 * main - prints digit numbers of base 10 starting from 0.
 *
 * Return: (0) success
 */

int main()
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d\n", i);
	}

	return (0);
}
